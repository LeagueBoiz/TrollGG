spring:
  profiles:
    active:
    default: local
    ##h2 데이터
    datasource:
      username: sa
      url: jdbc:h2:./extjsprac;AUTO_SERVER=TRUE
      driverClassName: org.h2.Driver
      password: ''

riot:
  api:
    key: ENC(mICQ0jITfFdZ1PNd2wxGtkWVslzkmROg5tFEPiKD6fxam9IJsYGLqgRmm00Wljz4CZXo1Cw316g=)
    ##riot key 입력

server:
  port: 8080

jasypt:
  encryptor:
    bean: jasyptStringEncryptor



---

spring:
  profiles:
    group:
      "local":
        -local-db
        -common
      "dev":
        -dev
        -local-db
      "prod":
        -prod
        -local-db
---

spring:
  config:
    activate:
      on-profile: "local-db"

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:trollGG?serverTimezone=Asia/Seoul
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
---

spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

---

spring:
  config:
    activate:
      on-profile: prod

  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8083




